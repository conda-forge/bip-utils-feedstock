{% set name = "bip-utils" %}
{% set version = "2.11.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ebellocchia/bip_utils/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 96bf16fed57d0837890314ec7e67c9ad8a6effdc62f9149139189bd22a0471e3
  patches:
    - fix-requirements-conda.patch

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vvv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - cbor2 ~=5.1
    - coincurve >=15.0.1,<20.0.0
    - crcmod ~=1.7
    - ecdsa ~=0.16
    - ed25519-blake2b >=1.4.1,<2.0.0
    - py-sr25519-bindings >=0.1.3,<2.0.0
    - pycryptodome ~=3.15
    - pynacl ~=1.5
    - python >={{ python_min }}
    - pytoniq-core

test:
  imports:
    - bip_utils
  requires:
    - pip
    - pytest
    - python {{ python_min }}
  commands:
    - pip check
    - python -m pytest -v tests
  source_files:
    - tests

about:
  home: https://github.com/ebellocchia/bip_utils
  summary: 'BIP Utility Library'
  description: |
    This package allows generating mnemonics, seeds, private/public keys and addresses for different types of cryptocurrencies.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - MementoRC
